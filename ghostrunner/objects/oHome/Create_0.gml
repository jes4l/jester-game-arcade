// Create Event
box_width = 40;
box_height = 22;

// Center horizontally
x1 = (room_width - box_width) / 2;
x2 = x1 + box_width;

// Center vertically with offset
offset_y = 20;  // Adjustable vertical offset
y1 = (room_height - box_height) / 2 + offset_y;
y2 = y1 + box_height;
